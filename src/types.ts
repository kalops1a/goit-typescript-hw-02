export interface GalleryImage {
  id: string;
  urls: {
    regular: string;
    small: string;
  };
  description: string;
}

export interface Image {
  id: string;
  urls: {
    regular: string;
    small: string;
  };
  description: string;
}